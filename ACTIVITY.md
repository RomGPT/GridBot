# Activity

## 2026-06-09

Reframed the repository as `Free Educational Grid Bot`, focused on learning rather than live trading:

- renamed README copy away from AFree/Afri wording;
- added `src/educational_gridbot/` with grid-level and toy backtest helpers;
- added `examples/simple_backtest.py` for a safe local learning example;
- added `tests/test_grid.py` for basic helper coverage;
- added `pyproject.toml` for package metadata and pytest configuration;
- added `docs/backtest-plan.md` and `docs/strategy-template.md` for learning workflows;
- added `data/sample_prices.csv` with fake sample prices;
- added `.gitignore` for Python caches, local env files, and generated outputs.

Earlier on 2026-06-09:

- improved README with positioning, safety notes, badges, and a workflow diagram;
- added `config.example.env` with placeholder-only configuration names.

## 2026-06-07

Small maintenance note added as part of the daily repository check. Next useful step: keep setup notes current and avoid committing secrets or generated files.
